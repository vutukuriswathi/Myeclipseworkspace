package abc.mobile.tests;

public class CalendarTest {

	/*
	 * @Test public void test2() { Date currentDate = new Date(); Calendar
	 * calendar = Calendar.getInstance(); //
	 * currentDate.setTime(currentDate.getTime() - 30 * 60000); // Date
	 * modifiedDate = calendar.getTime(); Calendar cal = Calendar.getInstance();
	 * cal.setTime(currentDate); cal.add(Calendar.HOUR, (int) -0.5); Date
	 * modifiedDate = cal.getTime(); DateTime dateTime = new DateTime();
	 * dateTime = dateTime.minusMinutes(30); Date modifiedDate1 =
	 * dateTime.toDate(); System.out.println("modified time is" +
	 * modifiedDate1);
	 * 
	 * }
	 */

}
